# Tasks API

- `POST /api/v1/projects/{projectId}/tasks` — Admin, Manager
- `GET /api/v1/projects/{projectId}/tasks` — Project access
- `GET /api/v1/tasks/{taskId}` — Project access
- `PUT /api/v1/tasks/{taskId}` — Admin, Manager
- `DELETE /api/v1/tasks/{taskId}` — Admin, Manager
