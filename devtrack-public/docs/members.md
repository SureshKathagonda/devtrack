# Project Members API

- `POST /api/v1/projects/{projectId}/members` — Admin, Manager
- `GET /api/v1/projects/{projectId}/members` — Project access
- `DELETE /api/v1/projects/{projectId}/members/{userId}` — Admin, Manager

Project owners cannot be removed from their own projects.
